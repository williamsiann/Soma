// src/plugins/vuetify.js

import { createVuetify } from 'vuetify';
import 'vuetify/dist/vuetify.min.css';

const vuetify = createVuetify({
  // Configuración opcional de Vuetify
});

export default vuetify;
